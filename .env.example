NODE_ENV=production
PORT=4174
PUBLIC_ORIGIN=https://westforge.dev
# Optional: set when the frontend is hosted separately from the API.
# Example: https://api.westforge.dev
VITE_API_BASE_URL=
# Optional: secret used to sign admin sessions. If omitted, a dev fallback is used.
ADMIN_SESSION_SECRET=
# Generate with: openssl rand -base64 32
CONTACT_ENCRYPTION_KEY=
ADMIN_USERNAME=codex
ADMIN_PASSWORD=westforge-dev-admin
